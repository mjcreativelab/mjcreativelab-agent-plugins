<!--
構成の選択（SKILL.md 7-N 参照）:
- 標準構成（既定）: 機能実装・複数ファイルにまたがる変更・設計判断を含む変更
- 簡易構成: chore / docs / typo / 依存更新・少量の変更など、レビュワーが diff だけで把握できる内容（図・表は不要）
- 標準構成でも各要素（図・表・セクション）は説明価値がある場合のみ使う。埋めるための図表は作らない
- プロジェクトに PULL_REQUEST_TEMPLATE（.github/ 配下）がある場合はそのセクション構成を骨格にし、標準構成の図表を対応するセクションへ埋め込む
-->

# 標準構成（既定）

## 概要

<!-- 背景・目的を 1〜3 文。続けて以下を箇条書きでリンクする（該当するもののみ）:
- チケット/Issue（Jira: [KEY-123](URL) 等）・親チケット
- 前提 PR / 関連 PR（スタックしている場合はレビュー・マージ順を ⚠️ で明示）
- 設計ドキュメント・実装ノート -->

## 進捗リスト / TODO

<!-- 実装済み項目を [x]、未対応の follow-up・レビュー反映を [ ] で列挙。サブタスク分割がなければ省略可 -->

## 実装内容

<!-- 以下から説明価値のある要素を選んで使う（全部は必須ではない） -->

<!-- 構成・アーキテクチャ（任意）: モード切替・コンポーネント関係など「構造」は mermaid flowchart で -->

<!-- クラス / ファイル構成（推奨）: 表で一覧する
| レイヤ | クラス / ファイル | 役割 |
|---|---|---|
-->

<!-- 主要フロー（推奨）: 処理の流れは mermaid sequenceDiagram で。正常系/異常系の分岐は alt/else で表現する -->

## 設計判断（要点）

### 採用した方針

<!--
| 観点 | 採用案 | 理由 |
|---|---|---|
-->

### 未対応（follow-up）

<!-- 無ければセクションごと省略
| 項目 | 理由 | 対応先 |
|---|---|---|
-->

## レビュースコープ

**重点レビュー**:
<!-- レビュワーに集中して見てほしい箇所（ファイル・観点） -->

**対象外**:
<!-- スコープ外（別チケット・別 PR）を明示 -->

## 動作確認

### 受入条件（期待結果）

<!-- 受け入れ基準 ↔ 検証手段（テスト名・コマンド）の対応表
| # | 受け入れ基準 | 検証 |
|---|---|---|
-->

### 確認手順

```bash
# レビュワーが手元で再現できるコマンド
```

## 備考

<!-- 運用上の注意・マージタイミング・レビュー済み表記など。不要ならセクションごと省略 -->

<!-- 関連 Issue が無ければ以下のセクション全体を省略 -->
<!-- 完全解決時は Closes、参照のみ/部分対応は Refs。他セクションの地の文で close/fix/resolve 系の語を Issue 番号に直接続けない（意図しない auto-close を避ける） -->
## 関連 Issue

Closes #XX

---

# 簡易構成（chore / 少量の変更）

## 概要
<!-- なぜこの変更をしたか 1〜3文 -->

## 変更内容
- ...

## レビュアー向け補足
<!-- 設計判断の理由、影響範囲、既知の制約、テスト方針、注目ポイント -->
<!-- 不要ならセクションごと省略 -->

<!-- 関連 Issue が無ければ以下のセクション全体を省略 -->
## 関連 Issue
Closes #XX
